// use std::ffi::CString;

// use extendr_engine::with_r;
// use libR_sys::*;

fn main() {
    //     with_r(|| unsafe {
    //         let bad_code = CString::new("%s").unwrap();
    //         let bad_string = Rf_protect(Rf_mkString(bad_code.as_ptr()));
    //         Rf_PrintValue(bad_string);

    //         Rf_unprotect(1);
    //     });
}

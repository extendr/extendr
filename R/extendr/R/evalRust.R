
evalRust <- function(
    code,
    depends = character(),
    plugins = character(),
    includes = character(),
    rebuild = FALSE,
    cacheDir = getOption("extendr.cache.dir", tempdir()),
    showOutput = verbose,
    verbose = getOption( "verbose" )
) {

}
